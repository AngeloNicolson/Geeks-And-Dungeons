const replyChain = (id) => {
  const fetchReplies = async () => {
    try {
    } catch (error) {
      console.error(error);
    }
  };

  return (
    <>
      <div>replyChain</div>
    </>
  );
};

export default replyChain;
